import { StrrikethroughDirective } from './strrikethrough.directive';

describe('StrrikethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrrikethroughDirective();
    expect(directive).toBeTruthy();
  });
});
